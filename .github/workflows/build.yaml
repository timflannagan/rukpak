name: build

on:
  # push:
  #   branches:
  #     - '**'
  pull_request:
    paths:
      - '**'

jobs:
  # build:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v2
  #     - uses: actions/setup-go@v2
  #       with:
  #         go-version: '~1.17'
  #     - run: make build

  image:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v1
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
      - name: Build the container image
        uses: docker/build-push-action@v2
        id: docker_build
        with:
          context: .
          file: Dockerfile
          push: false
          tags: quay.io/operator-framework/rukpak:latest
      - name: Verify new tags
        run: docker run quay.io/operator-framework/rukpak:latest

